{
  "api": "bedrock-runtime:Converse (via Strands BedrockModel)",
  "region": "us-east-1",
  "modelId": "openai.gpt-oss-120b-1:0",
  "framework": "strands-agents 1.50.2 (BedrockModel)",
  "streaming": false,
  "maxTokens": 2048,
  "tools": [
    {
      "name": "retrieve_explanation_bundle",
      "description": "Retrieve the deterministic, captured Neo4j ExplanationBundle by insight ID.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "insightId": {"type": "string", "description": "Exact insight ID to retrieve."}
        },
        "required": ["insightId"]
      }
    }
  ],
  "systemPromptSummary": "Evidence-bound rowing coach with Pegasus context and strict CoachOutput constraints",
  "userMessage": "Create coaching for insightId \"insight-rate-without-power\".",
  "inputSources": {
    "explanationBundle": "graph/cache/explanation-bundle.json (source: cached-neo4j)",
    "pegasusEvidence": "artifacts/twelvelabs/pegasus-multipass-summary.json (candidateInsights + rejectedClaims)"
  },
  "reproducibleCommand": "PYTHONPATH=/path/to/wake /path/to/wake/.venv/bin/python /path/to/wake/.claude/jobs/run_e2e.py",
  "notes": {
    "strandsModel": "BedrockModel (uses bedrock-runtime:Converse natively, NOT OpenAIResponsesModel which requires Mantle)",
    "evidenceFlow": "Pegasus candidateInsights provided in system prompt context; Neo4j ExplanationBundle retrieved via tool call during agent execution",
    "claim": "This run proves OpenAI synthesized reviewed Pegasus and Neo4j evidence into coaching. It does NOT claim that OpenAI dispatched the Pegasus investigations."
  }
}
